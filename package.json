{
  "name": "throttlr",
  "version": "1.0.0",
  "description": "Rate limiting middleware for Express. Fixed window vs sliding window, with interactive exploit demo.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx server.ts",
    "test": "tsx --test tests/*.test.ts"
  },
  "keywords": ["rate-limiting", "express", "middleware", "sliding-window"],
  "author": "Simon Kimeu",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  }
}
